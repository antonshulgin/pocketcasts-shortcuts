# Pocketcasts Shortcuts

A Chrome extension that extends [Pocketcasts](https://play.pocketcasts.com) web interface with some more shortcuts:

- `j` — skip back

- `k` — play/pause

- `l` — skip forward

- `m` — mute/unmute

- `-`/`=` — decrease/increase volume
